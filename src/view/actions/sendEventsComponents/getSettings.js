/**
 * (c) Facebook, Inc. and its affiliates. Confidential and proprietary.
 */
export default (
    {
        isTestEvent,
        testEventCode,
        event_name,
        event_time,
        event_source_url,
        opt_out,
        event_id,
        action_source,
        em,
        ph,
        ge,
        db,
        ln,
        fn,
        ct,
        st,
        zp,
        country,
        external_id,
        client_ip_address,
        client_user_agent,
        fbc,
        fbp,
        subscription_id,
        fb_login_id,
        lead_id,
        value,
        currency,
        content_name,
        content_category,
        content_ids,
        contents,
        content_type,
        order_id,
        predicted_ltv,
        num_items,
        search_string,
        status,
        delivery_category
  }
) => ({
    isTestEvent,
    testEventCode,
    event_name,
    event_time,
    event_source_url,
    opt_out,
    event_id,
    action_source,
    em,
    ph,
    ge,
    db,
    ln,
    fn,
    ct,
    st,
    zp,
    country,
    external_id,
    client_ip_address,
    client_user_agent,
    fbc,
    fbp,
    subscription_id,
    fb_login_id,
    lead_id,
    value,
    currency,
    content_name,
    content_category,
    content_ids,
    contents,
    content_type,
    order_id,
    predicted_ltv,
    num_items,
    search_string,
    status,
    delivery_category
});
